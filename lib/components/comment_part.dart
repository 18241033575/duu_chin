import 'package:flutter/material.dart';


class CommentPart extends StatelessWidget {

  const CommentPart({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('comment part'),
    );
  }

}